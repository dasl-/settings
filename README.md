# settings
## .zshrc
Depends on:
* oh-my-zsh https://ohmyz.sh/#install
* other stuff too?
```
brew install zsh
brew install fzf
brew install zsh-syntax-highlighting
```

## .gitconfig
Depends on:
```
brew install git-delta
```
